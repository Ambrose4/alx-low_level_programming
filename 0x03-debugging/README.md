C - Debugging

FILES

0. Multiple mains
We typically just provide you with one primary file per project to test with.
In this main file, for instance, there is a test for the positive or negative() method that you used in a previous C project. 

1. Like, comment, subscribe
Copy the primary document.
The section of code that is causing the output to enter an infinite loop should be commented out (don't delete it!).


We'll check your line count, so don't add or remove any lines of code.
You can only remove existing code by adding a remark.

For this work, you do not need to compile with -Wwall -Werror -Wextra -pedantic. 

2. 0 > 972?
This program prints the largest of three integers.

3. Leap year
This program converts a date to the day of year and determines how many days are left in the year, taking leap year into consideration.


